---
AIGC:
    Label: "1"
    ContentProducer: 001191440300708461136T1XGW3
    ProduceID: 72c3809ef190e42ccac0819594be7524_ac647bdf9dfd11f1a54f525400f8a581
    ReservedCode1: B1nKCNrSQFmDXQaLQiUo+Em8rfPzAPRVtF7gqNB62OK4HNLBZwhFedVVwUObhLk/BjOEDTAdSHrWtIaC4PE9AVenaIiQQEgPuS6qhgNS6beavfIJiD2hBC6G0MQSk4LmrEIHSisupugoB7QI7D3+81tpHohoA/+awdfmbvtM9vCyNJSyfX8VRFwksyk=
    ContentPropagator: 001191440300708461136T1XGW3
    PropagateID: 72c3809ef190e42ccac0819594be7524_ac647bdf9dfd11f1a54f525400f8a581
    ReservedCode2: B1nKCNrSQFmDXQaLQiUo+Em8rfPzAPRVtF7gqNB62OK4HNLBZwhFedVVwUObhLk/BjOEDTAdSHrWtIaC4PE9AVenaIiQQEgPuS6qhgNS6beavfIJiD2hBC6G0MQSk4LmrEIHSisupugoB7QI7D3+81tpHohoA/+awdfmbvtM9vCyNJSyfX8VRFwksyk=
---

# AI 音乐工作平台 · 黑客松参赛方案（48h 修订版）

> 暂定名：待定（建议贴合"人主导 + AI 辅助"理念命名）
> 参赛：Rebuild 黑客松 ｜ 定位：AI 赋能、人工为主的可编辑音乐工作平台
> 版本：v2（评审修订）｜ 核心变化：MVP 收敛为"一条修音准闭环"，补齐 Agent schema、音乐分析层、demo 脚本与 Plan B

---

## 〇、本版修订摘要

针对上一版评审意见，本版做以下收敛：

- 首选用户收敛为"音乐爱好者/短视频创作者"，唯一场景为"人声修音准"
- MVP 从五条必须收敛为一条链路：导入 → 音乐分析 → 自然语言 → 可编辑操作 → 渲染 → 前后对比 → 回退
- 明确不做：MusicGen、Demucs（改为加分项）、双模式时间轴、实时效果链、模板库、打包发布
- 补齐修音准技术链路：librosa 小节定位 + pyin 音高检测 + Phase Vocoder 参数化修正 + 重检测验证
- 补齐 Agent 工具 schema、LLM 输出示例、预检规则、失败状态与回退机制
- 补齐 48 小时真实排期、每半天退出标准、demo 脚本与 Plan B
- 修订"范式"表述：不宣称发明节点画布，只论证"自然语言 → 可审计音乐分析 → 可编辑操作序列"这一条闭环

---

## 一、产品定位

**一句话定位**：一个以人工为主导、AI 为辅助的 AI 音乐工作平台；用户用自然语言表达想法，Agent 把想法转成可逐条确认、可编辑、可回放、可量化的修音操作序列。

**核心理念**：
- 过程可编辑：AI 先产出可解释的操作步骤，用户确认/修改/撤销后才渲染
- 操作可回放：节点序列即"音乐程序"，可保存、改参、复用，而不是一次性黑盒输出
- 结果可验证：修音前后有可听的 A/B 和可测的音高偏差数字

**范式的诚实表述**：节点画布本身不新，TuneFlow、ComfyUI 都有。我们验证的新东西是"自然语言 → 音乐分析层（小节/音高 grounding）→ 参数化可编辑操作序列"的人机协同方式；关键不在界面，而在操作可解释、可修改、可回放。

---

## 二、痛点与差异化

| 痛点 | 现状 | 我们的解法 |
|------|------|-----------|
| AI 修音黑盒 | 一键修音结果不可编辑，错了只能重来 | 修音 Agent 产出参数化操作序列，逐条确认 |
| 经验无法复用 | 修音效果无法沉淀为可复用流程 | 操作序列即模板，可保存可改参（赛后方向） |
| 工具割裂 | 分析、修音、对比分散在不同工具 | 一条链路在统一画布完成 |

**对标差异（诚实版）**：
- TuneFlow：AI 原生 DAW，功能全，AI 能力以插件/预设形式提供，面向专业用户；我们不比功能，比"对话生成、逐条确认、可回放操作序列"这一交互闭环
- ComfyUI：节点编排成熟，但面向技术用户，缺音乐编辑体验；我们补自然语言入口和修音编辑体验
- 现有 LLM→脚本/宏方案：生成代码或宏，中间结果不可听、不可逐条校验；我们让每个操作有音频实体，可试听、可改参、可回退

---

## 三、目标用户与单一场景

**首选用户**：音乐爱好者与短视频创作者。他们会录到/收到不完美的人声，不愿学 DAW，又信不过一键 AI 修音；需要"我说一句话，它能给出可检查、可改、可重来的修音方案"。

**北极星指标（demo 可测）**：
- 从自然语言输入到听到结果 ≤ 60 秒
- 修正后平均音高偏差（cents）明显下降，且用户可改参数重渲染

**唯一 demo 场景**：
> 导入一段带轻微跑调的单音人声 WAV → 输入"只把人声第 3 小节高音修准" → Agent 解析 → 音乐分析层定位第 3 小节与音高偏移 → 生成 1 条 pitch-correction 操作 → 用户确认并调整强度 → 渲染 → A/B 播放 + 前后偏差曲线对比 → 回退重来。

**为什么选它**：修音准是"白盒可编辑"最好的证明：错误客观可测（音高偏差），操作白盒（参数可改），可回退，且不依赖 GPU 与大数据下载。

---

## 四、MVP 范围（48 小时收敛版）

**必须完成（最小闭环）**：
1. 导入/播放/波形（WaveSurfer.js）
2. 音乐分析节点：小节定位 + 音高轨迹（librosa + pyin）
3. 修音 Agent：自然语言 → JSON 结构化操作 → 用户确认
4. 修音准执行节点：Phase Vocoder 参数化修正 + 重检测
5. 前后对比与导出 WAV

**明确不做（本周末）**：
- MusicGen 音乐生成、Demucs 扒歌（作为加分项）
- 双模式时间轴（只做节点画布 + 节点内波形）
- 实时效果链渲染（统一离线渲染，避免预览与导出不一致）
- 多轮对话、模板库、MIDI 转写
- Electron 打包分发（开发环境跑通即可，浏览器版作为兜底）

**砍功能标准**：任何模块若超过半天预算，先接"预置规则路径"保住闭环，再回来替换为真实实现。

---

## 五、修音 Agent 详细设计

### 5.1 工作流程

```
用户自然语言
  → 意图解析（LLM，JSON Schema 约束）
  → 音乐分析层：小节/拍点定位 + 音高轨迹（作为 grounding 上下文）
  → 计划生成：带精确坐标与参数的操作序列
  → 预检：schema + 语义校验 + 目标节点锁定检查
  → 用户逐条确认/修改参数
  → 执行渲染 → 重检测验证 → 可回退
```

### 5.2 工具 schema（示例）

```json
{
  "name": "correct_pitch",
  "description": "修正指定时间范围内人声的音准",
  "parameters": {
    "type": "object",
    "properties": {
      "track": {"type": "string"},
      "start_sec": {"type": "number", "minimum": 0},
      "end_sec": {"type": "number", "minimum": 0},
      "mode": {"type": "string", "enum": ["auto", "scale"]},
      "scale": {"type": "string", "enum": ["C", "C#", "D", "D#", "E", "F", "F#", "G", "G#", "A", "A#", "B"]},
      "correction_strength": {"type": "number", "minimum": 0, "maximum": 1}
    },
    "required": ["track", "start_sec", "end_sec", "mode"]
  }
}
```

**LLM 输出示例**：

```json
{"op": "correct_pitch", "track": "vocals", "start_sec": 24.8, "end_sec": 31.2, "mode": "auto", "correction_strength": 0.8}
```

时间坐标来自音乐分析层，不由 LLM 猜测；LLM 只负责意图映射与参数翻译。

### 5.3 音乐分析层（新增，评审补齐）

- librosa `beat.beat_track` / onset：估算小节边界，"第 3 小节" → 时间窗口
- librosa `pyin`：帧级音高轨迹与置信度，计算每音符偏移量
- 分析结果作为节点元数据展示在画布上，边界可拖动修正
- 定位失败时降级：跳过小节定位，Agent 改为"整段高音修正"

### 5.4 修音准执行链路（新增，评审补齐）

1. 检测：pyin 帧级音高（hop 默认 20ms），按音符分段取中值
2. 修正：Phase Vocoder（librosa `effects.pitch_shift`）逐段修正，默认限制 ±2 个半音，超出阈值拒绝执行
3. 验证：修正后重新检测，输出"修正前/后偏差曲线 + 平均 cents 偏差"
4. 质量兜底：修正量超阈值或检测置信度过低时不执行，提示用户调整素材或缩小范围；绝不静默硬改

### 5.5 状态机（修订版）

`待解析 → 已生成计划(待确认) → 部分修改 → 已确认 → 执行中 → 已渲染 → 已回退`

新增状态与规则：
- 解析失败、预检失败、执行失败、已取消：全部保留操作日志，可人工接管
- 历史为分支式：从任意已渲染节点 fork 新修改，不覆盖旧结果
- 执行期间锁定被修改节点，用户可取消，取消后回到可编辑状态

### 5.6 与人工编辑的双向同步

- 节点图是唯一事实来源；音乐分析结果以元数据节点形式进入画布
- 预检规则：track 存在、时间范围在音频长度内、参数在 bounds 内、目标节点未锁定、修正量不超阈值
- 用户手动修改节点后，Agent 以最新节点状态为输入生成新计划，并对变更做 diff 提示

---

## 六、技术架构（48h 版）

```
┌─────────────────────────────────────────┐
│ 前端层（Electron 壳 + React）            │
│  - React Flow 节点画布                    │
│  - WaveSurfer.js 波形/选区               │
│  - 对话输入框（Agent 交互入口）           │
├─────────────────────────────────────────┤
│ Agent 层                                │
│  - LLM 意图解析 + JSON Schema 约束       │
│  - 计划预检 / 状态管理                   │
├─────────────────────────────────────────┤
│ 音乐分析层（Python worker）              │
│  - librosa 小节/拍点定位                 │
│  - pyin 音高轨迹                         │
├─────────────────────────────────────────┤
│ 音频处理层（Python worker）              │
│  - Phase Vocoder 参数化修音              │
│  - 重检测验证 / WAV 导出                 │
├─────────────────────────────────────────┤
│ 数据层                                  │
│  - 工程 JSON（节点图 + 元数据 + 历史）    │
│  - 本地资产目录 / 自动保存               │
└─────────────────────────────────────────┘
```

**架构要点（修订）**：
- 分析、渲染全部在 Python worker，崩溃不影响 UI
- 统一离线渲染：预览就是渲染结果，不维护第二套实时图，从根上避免"预览和导出不一致"
- 不依赖 GPU，赛前无需下载大模型，规避现场硬件与网络风险

---

## 七、技术选型（修订版）

### 7.1 前端与画布

| 组件 | 主选 | 说明 |
|------|------|------|
| 前端框架 | React 18 | 生态最全 |
| 节点画布 | React Flow | 事实标准，自定义节点成熟 |
| 波形 | WaveSurfer.js | 选区、区域标注、缩放、渲染 |
| 状态管理 | Zustand | 轻量，适合节点图 + 工程状态 |
| 桌面壳 | Electron | 本地文件优先；核心链路先在浏览器跑通，Electron 只是壳 |

### 7.2 音频处理（Python worker）

| 组件 | 主选 | 说明 |
|------|------|------|
| 音频 IO | soundfile + numpy | 无 GPU，体积小，WAV 读写 |
| 小节/拍点 | librosa | `beat.beat_track` / onset |
| 音高检测 | librosa `pyin` | 单音人声足够，免装 TensorFlow；CREPE 作为赛后备选 |
| 音高修正 | librosa `effects.pitch_shift` | Phase Vocoder；限制 ±2 半音 |
| 验证 | pyin 重检测 | 输出 cents 偏差曲线 |
| 导出 | soundfile | 先只导出 WAV，mp3 后用 FFmpeg |

### 7.3 Agent 层

| 组件 | 主选 | 说明 |
|------|------|------|
| LLM | OpenAI / DeepSeek API | 只做意图映射，坐标来自分析层 |
| 参数约束 | JSON Schema / zod | 结构 + 语义双层校验 |
| 兜底 | 规则解析模板 | 断网时"第 N 小节高音修准"离线走通同一 UI |

### 7.4 参考实现

| 项目 | 借鉴点 |
|------|--------|
| TuneFlow | AI 原生 DAW 的产品形态，明确我们的差异是"可编辑操作序列" |
| Mecchi | React Flow 封装音频模型节点 |
| Audio Editor App | 仅借鉴节点 + 波形展示，不做双模式 |

### 7.5 运行时成本（修订后）

- Python worker 依赖：`librosa`、`soundfile`、`numpy`、`scipy`（无 torch、无 TensorFlow、无 GPU 需求）
- 素材规格：30 秒单轨人声 WAV，全链路目标 ≤ 60 秒
- 安装体积：百 MB 级，赛前锁定 Python 3.11 + 固定 requirements 版本

---

## 八、48 小时排期（可执行版）

**赛前（提前 2 天，必做）**：
- 锁定环境：Python 3.11 + 固定版本 requirements，跑通"检测 → 修正 → 重检测"脚本并记录 baseline
- 准备 demo 素材：2 段 30 秒单音人声（一段轻微跑调、一段跑调较重）
- 录制 30 秒兜底渲染素材与截图

**Day 1：画布 + 分析层**
- 上午：React/Vite + React Flow + WaveSurfer 导入/播放
- 下午：Python worker + librosa 小节定位 + pyin 音高轨迹
- 晚上：分析结果上画布（显示第 3 小节窗口）
- 里程碑：素材可导入并显示检测到的小节窗口
- 退出标准：未达成则次日降级为"整段修音"，不做小节定位

**Day 2：Agent + 修音执行**
- 上午：Agent schema + LLM 意图解析 + 预检
- 下午：Phase Vocoder 修音节点 + 重检测 + A/B 对比
- 晚上：全链路联调（输入 → 确认 → 渲染 ≤ 60 秒）与回退
- 里程碑：一条自然语言指令全链路走通

**Day 3：demo 打磨**
- 上午：demo 脚本、Plan B、路演视频录制
- 下午：全流程走查 5 次并计时
- 晚上（如有富余）：Demucs 去人声加分项或 UI 打磨

**加分项排序**：1 真实小节定位；2 Demucs 去人声；3 多轮修改；4 模板保存。落后就按此顺序砍。

---

## 九、Demo 脚本与 Plan B

**Demo 脚本（3 分钟）**：
1. 0:00-0:20 一句话定位："修音不黑盒，每一步都能看、能改、能重来"
2. 0:20-1:00 导入素材，播放，展示检测到的第 3 小节窗口
3. 1:00-1:30 输入"只把人声第 3 小节高音修准"，展示 1 条可编辑操作与参数
4. 1:30-2:00 确认执行，进度条显示，渲染完成后 A/B 播放
5. 2:00-2:30 展示修正前后音高偏差曲线（数字下降），拖动强度重渲染一次
6. 2:30-3:00 回退演示 + 收尾

**Plan B**：
- 无 GPU：全链路 CPU 即可，这是选型底线
- 断网/API 失败：规则解析模板离线走通同一 UI
- 修音效果差：赛前验证素材，现场备 2 段素材轮换
- 渲染超时：素材 30 秒封顶，超时自动降采样 16kHz 兜底
- Electron 异常：浏览器版兜底，Electron 只是壳

---

## 十、风险与规避（修订版）

| 风险 | 规避 |
|------|------|
| 音高修正质量不达标 | 单音人声素材、修正量限 ±2 半音、赛前 baseline + 双素材备选 |
| 小节定位不精确 | 检测边界可视化可拖动；失败降级整段修音 |
| LLM 解析失败/断网 | JSON Schema + 规则模板双通道 |
| 48 小时工期紧 | MVP 仅一条链路，每半天有退出标准，砍项顺序已定 |
| 预览与导出不一致 | 统一离线渲染管线，预览即渲染结果 |
| 版权合规 | 演示素材自录/无版权；产品阶段对"扒歌"给出个人学习与权利声明边界 |
| 分发体积大 | 本版不做打包；产品阶段再评估模型服务化与小模型方案 |

---

## 十一、赛前证据清单

- [ ] "检测 → 修正 → 重检测"脚本跑通，输出 baseline cents 数字
- [ ] 第 3 小节窗口检测截图一张
- [ ] 一份真实 LLM 输出示例（含 1 个错误案例）
- [ ] 2 段 30 秒 demo 素材与标注
- [ ] 3 分钟 demo 脚本计时表

答辩口径：以上即"我们已经验证的部分"，与 demo 一一对应。

---

## 十二、参赛亮点总结（诚实版）

1. 白盒修音准闭环：自然语言 → 可编辑参数 → 可量化前后对比 → 可回退
2. 音乐分析层 grounding：LLM 不猜时间，坐标来自可审计的分析节点
3. 可回放操作序列：操作即数据，可保存、改参、复用（赛后方向）
4. 48 小时可交付：明确不做清单 + 每半天退出标准 + Plan B
