# AI 音乐工作平台 · 后端任务清单（后端 B）

> 范围：Python worker（音乐分析 + 修音渲染）+ Agent 意图解析 + WAV 导出 + IPC 服务端
> 前端只负责界面/交互；本清单不含任何前端任务（React Flow / WaveSurfer / 对话面板等）
> 对应：参赛方案 v2 + 制作方案（双人分工版）v2.0

---

## 一、接口契约（后端必须先定义，前后端对齐）

| 通道 | 方向 | 关键字段 |
|------|------|----------|
| `analyze` | 请求 | `{track_id, file_path}` |
| `analysis_result` | 响应 | `{bars:[{index,start_sec,end_sec}], pitch:[{t,pitch,confidence}]}` |
| `parse_intent` | 请求 | `{text, project_state}` |
| `plan` | 响应 | `{op:"correct_pitch", track, start_sec, end_sec, mode, scale, correction_strength}` |
| `execute_plan` | 请求 | `{plan_id, parameters}` |
| `render_result` | 响应 | `{output_path, before_cents, after_cents, curve}` |
| `project.json` | 工程文件 | `{nodes, analysis, history, version}` |
| 错误 | 统一 | `{error_code, message}` |

约束：时间单位**秒**；音频 **44.1kHz WAV**；强度 **0–1**；默认修正量 **±2 半音**；分析/渲染全部走 Python worker，崩溃不影响 UI。

---

## 二、后端任务清单（按阶段）

### 阶段 0 · 赛前准备（D-2 ~ D-1，必做，退出标准）
- [ ] **B0 后端工程骨架 + IPC 服务端** — 搭建 Python worker 工程；实现 `analyze/analysis_result/parse_intent/plan/execute_plan/render_result` 六通道；worker 启动、心跳、日志、退出清理。
- [ ] **B1 环境锁定 + baseline 脚本** — 锁 Python 3.11 + 固定 requirements（librosa/soundfile/numpy/scipy，无 torch/TF/无 GPU）；跑通「读入→pyin 检测→pitch_shift 修正→重检测」；记录 **baseline 平均 cents 偏差 + 单次耗时**。
- [ ] **B2 demo 素材** — 2 段 30 秒单音人声 WAV（轻微跑调 / 较重跑调）+ 标注，用于联调与现场轮换。

### 阶段 1 · Day 1 音乐分析层
- [ ] **B3 小节定位** — librosa `beat.beat_track` + onset 估算小节边界，输出 `bars` 列表；「第 3 小节」→ 时间窗口。
- [ ] **B4 音高轨迹** — librosa `pyin` 帧级音高+置信度（hop≈20ms），输出 `pitch` 数组与低置信区段；按音符分段取中值算偏移。
- [ ] **B5 分析 JSON 对接前端** — `analysis_result` 稳定输出；联调 M1：30 秒素材分析 **≤15 秒**，第 3 小节边界与时间轴对齐；失败返回错误码供前端降级。

### 阶段 2 · Day 2 Agent + 修音执行
- [ ] **B6 Agent 意图解析 + 预检** — `correct_pitch` 工具 schema；LLM 返回结构化 JSON；JSON Schema + 语义双层预检（track 存在 / 时间范围合法 / 参数 bounds / 节点未锁 / 修正量 ≤±2 半音，越界拒绝）；产出真实 LLM 示例（含 1 错误案例）。
- [ ] **B7 Agent 兜底：规则模板** — 解析「第 N 小节高音修准」，断网/API 失败离线走通同一 UI 与同一 plan 字段（双通道兜底）。
- [ ] **B8 修音执行：Phase Vocoder** — 裁切窗口→pyin 检测→逐音符分段→`effects.pitch_shift` 修正，±2 半音限制，超阈值拒绝；质量不达标绝不静默硬改。
- [ ] **B9 重检测验证：偏差曲线** — 修正后重检测，输出 before/after 偏差曲线 + 平均 cents；验证下降且可听。
- [ ] **B10 导出 WAV** — soundfile 将修正段合成回原音频，输出 `output_path`；确认字节正确可播放；统一离线渲染（预览即渲染结果）。

### 阶段 3 · Day 3 打磨与兜底
- [ ] **B11 稳定性：5 次全链路** — 连续跑全链路，记录耗时/失败点，修复崩溃补日志；错误码统一 `{error_code, message}`；全链路 ≤60 秒（渲染 ≤30 秒）。
- [ ] **B12 渲染优化与兜底** — 16kHz 降采样兜底、缓存中间结果、WAV 字节校验；规则模板离线验证；预生成 1 条兜底渲染结果备用。

---

## 三、后端里程碑与退出标准

| 节点 | 时间 | 后端里程碑 | 未达成降级 |
|------|------|-----------|-----------|
| M1 | Day 1 晚 | 分析结果上图（第 3 小节窗口） | 次日「整段修音」，不做小节定位 |
| M2 | Day 2 晚 | 一条自然语言全链路 ≤60 秒 | 规则模板 + 预生成渲染保 demo |
| M3 | Day 3 下午 | 5 次走查全过 | 路演视频 + Plan B 素材顶上 |

加分项顺序（砍项从低到高）：模板保存 → 多轮修改 → Demucs 去人声 → 真实小节定位。
原则：**先保「一句话 → 可听结果」闭环，再谈加分项；砍功能不砍验收。**

---

## 四、建议开工顺序
1. 先 B0（骨架+IPC）+ B1（环境+baseline）并行搭地基，赛前必须完成。
2. Day1 主推 B3/B4/B5 分析层，确保 M1。
3. Day2 主推 B6–B10，确保 M2。
4. Day3 只做 B11/B12 打磨与兜底，最后 1 小时不碰代码。
